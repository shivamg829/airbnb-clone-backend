<form action="/favouritelist" method="post" class="w-full">
  <input type="hidden" name="homeId" value="<%= home.id %>">
  <button type="submit" class="w-full bg-yellow-500 hover:bg-yellow-600 text-white font-semibold py-3 px-4 rounded-xl transition-all duration-300 transform hover:scale-105 hover:shadow-lg flex items-center justify-center gap-2">
    <span>Add to Favourites</span>
  </button>
</form>